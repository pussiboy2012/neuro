# Основные библиотеки для работы с PyTorch и компьютерным зрением
torch>=1.9.0
torchvision>=0.10.0

# Научные и графические библиотеки
numpy>=1.19.0
matplotlib>=3.3.0
pandas>=1.1.0
seaborn>=0.11.0

# Вспомогательные инструменты для прогресс-баров и таблиц
tqdm>=4.50.0
tabulate>=0.8.9

# Метрики и матрица ошибок
scikit-learn>=0.24.0

# Опционально – точный подсчёт FLOPs (рекомендуется, но код работает и без него)
ptflops>=0.7.0
